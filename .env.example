# =====================================================================
# X Intelligence & Content Automation Skills — Environment Template
# =====================================================================
# このファイルを `.env` にコピーし、必要な値を埋めて使用してください。
# `.env` は git 管理外です（.gitignore で除外済み）。
# =====================================================================

# --- Provider Selection ---------------------------------------------
# X 検索の Provider を切替。MVP では mock を既定推奨。
# 選択肢: mock | hermes | xai | x_api
X_SEARCH_PROVIDER=mock

# LLM Provider を切替。API key 未設定なら自動で mock fallback。
# 選択肢: mock | claude | grok
LLM_PROVIDER=mock

# --- xAI / Grok -----------------------------------------------------
XAI_API_KEY=
GROK_PROVIDER=xai            # xai | hermes
GROK_MODEL=grok-4            # 推奨現行モデル。最新は README 参照
GROK_IMAGE_MODEL=grok-2-image
GROK_VIDEO_MODEL=grok-imagine

# --- Hermes Agent (Pattern B / WSL2) --------------------------------
# Hermes credentials are managed by the CLI itself in ~/.hermes/.env on the
# WSL2 side. This project never reads or writes those secrets. The flags
# below configure how *we* call the Hermes CLI from Windows-side Python.
HERMES_ENABLED=false
HERMES_TIMEOUT_SECONDS=180
HERMES_RAW_RESPONSE_DIR=outputs/raw_responses/hermes
HERMES_FALLBACK_TO=mock           # mock | none (overridable via --search-fallback)
HERMES_CLI_INVOCATION=wsl bash -lc "hermes"   # how we shell into WSL2 for Hermes
HERMES_TOOLSETS=x_search          # comma-separated; default x_search only

# --- Anthropic (Claude) ---------------------------------------------
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-opus-4-7   # MVP 既定。要約・スコアリング用

# --- X API v2 (将来) ------------------------------------------------
X_SEARCH_ENABLED=false
X_API_BEARER_TOKEN=

# --- Grok Imagine (画像/動画生成) ----------------------------------
GROK_IMAGINE_ENABLED=false     # MVP では prompt 生成までで停止
GROK_IMAGINE_API_KEY=

# --- Misc -----------------------------------------------------------
TIMEZONE=Asia/Bangkok          # 柴田さんタイ滞在中。帰国後は Asia/Tokyo
DEFAULT_TIME_RANGE=24h         # 24h | 3d | 7d
LOG_LEVEL=INFO
